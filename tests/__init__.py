"""VocalizeAI test package.

Marked as a real Python package so test modules can import shared helpers
(e.g. ``from tests.dialogue_fixtures import load_scenarios`` in Phase 4
and ``from tests.conftest import make_scripted_llm`` for the scripted-LLM
multi-call helper) without depending on pytest's path-injection magic.
"""
